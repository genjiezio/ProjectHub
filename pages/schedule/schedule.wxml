<!--pages/schedule/schedule.wxml-->

<van-calendar
  title="日历"
  poppable="{{ false }}"
  show-confirm="{{ false }}"
  class="calendar"
  formatter="{{ formatter }}"
  min-date="{{ minDate }}"
  max-date="{{ maxDate }}"
/>
